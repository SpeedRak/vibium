from itertools import permutations
s = 'DEIKSTRA'
res = set()
for x in permutations(s):
    a = ''.join(x)
    a = a[:6]  #только 6 букв
    for c in 'DKSTR':
        if a.count('I') == 1 and ('I' + c in a): 
            res.add(a)
print(len(res))

#Ада составляет шестибуквенные слова из букв Д, Е, й, К, С, Т, Р, А.
#Буква й встречается в слове ровно один раз, и после неё обязательно идёт
#согласная. Буквы в слове не повторяются.
#сколько слов может составить Ада?
    