def f(n, k):
    res = 0
    step = 0
    for x in n[::-1]:
        res += int(x, 36) * k ** step
        step += 1
    return res

for p in range(5, 100):
    for q in range(6, 100):
        if f('234', p) == f('345', q):
            print(p, q)
            break
   
