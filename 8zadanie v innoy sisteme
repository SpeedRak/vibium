from itertools import product
s = '012345678'
k = set()
for x in product(s, repeat=7):
    a = ''.join(x)
    if a[0] != '0' and int(a[0]) % 2 == 0 and int(a[-1]) % 3 != 0 and a.count('6') > 0:
        k.add(a)
print(len(k))
#Определите количество семизначных чисел, записанных в
#девятеричной системе счисления, которые не начинаются с
#нечетных цифр, оканчиваются на цифры, не делящиеся на 3 без
#остатка, а также содержат в своей записи хотя бы одну цифру 6.