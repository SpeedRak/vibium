from math import *
k = 0
for i in open('9 1 file.csv'):
    x = list(map(int, i.split(';')))
    if len(x) != len(set(x)):
        continue #prof из библиотеки math, это как sum только произведение
    if (max(x) + min(x)) ** 2 < prod(x) // max(x) // min(x):
        continue
    k += 1
print(k)

#Откройте файл электронной таблицы, содержащей в каждой строке пять натуральных чисел. 
#Определите количество строк таблицы, содержащих числа, для которых выполнены оба условия:
#– все числа в строке различны;
#– квадрат суммы максимального и минимального чисел из строки не меньше произведения трёх оставшихся.
#В ответе запишите только число.