f = open('27 3 A.txt')
stars = []
for i in f:
    stars += [list(map(float, i.replace(',', '.').split()))]
clasters = {1:[], 2:[], 3:[]}
for star in stars:
    x, y = star
    if x < 6:
        clasters[1] += [star]
    elif x > 6 and y > 23:
        clasters[2] += [star]
    elif x > 6 and y < 23:
        clasters[3] += [star]
# print(len(stars))
# print (len(clasters[1]), len(clasters[2]), len(clasters[3]))
def dlina(star1, star2):
    x1, y1 = star1
    x2, y2 = star2
    return ((x2-x1)**2 + (y2 - y1)**2)**0.5
def centr(claster):
    mindlina = 10000000000000000
    for star1 in claster:
        summa = 0
        for star2 in claster:
            summa += dlina(star1, star2)
        if mindlina > summa:
            mindlina = summa
            result = star1
    return result
px = []
py = []
for claster in clasters:
    px += [centr(clasters[claster])[0]]
    py += [centr(clasters[claster])[1]]
#print(int((sum(px)/3)*10000), py)
px = int((sum(px) / 3) * 10000)
py = int((sum(py) / 3) * 10000)
print(px, py)

# Учёный решил провести кластеризацию некоторого множества звёзд по их расположению на карте звёздного неба. Кластер звёзд – это набор звёзд (точек) на графике, лежащий внутри прямоугольника высотой H и шириной W. Каждая звезда обязательно принадлежит только одному из кластеров.
# Истинный центр кластера, или центроид, – это одна из звёзд на графике, сумма расстояний от которой до всех остальных звёзд кластера минимальна.
# Под расстоянием понимается расстояние Евклида между двумя точками A(x1​,y1​)
# A(x1​​,y1​​) и B(x2​,y2​)
# B(x2​​,y2​​) на плоскости, которое вычисляется по формуле:
# d(A,B)= ((x2-x1)**2 + (y2 - y1)**2)**0.5
# ​​
# В файле A хранятся данные о звёздах трёх кластеров, где H=5, W=5 для каждого кластера. В каждой строке записана информация о расположении на карте одной звезды: сначала координата x, затем координата y. Значения даны в условных единицах. Известно, что количество звёзд не превышает 1000.
# В файле B хранятся данные о звёздах четырёх кластеров, где H=5, W=5 для каждого кластера. Известно, что количество звёзд не превышает 10 000. Структура хранения информации о звездах в файле B аналогична файлу А. 
# Для каждого файла определите координаты центра каждого кластера, затем вычислите два числа: Px
# Px​​ – среднее арифметическое абсцисс центров кластеров, и Py
# Py​​ – среднее арифметическое ординат центров кластеров.
# В ответе запишите четыре числа: в первой строке сначала целую часть произведения Px
# Px​​ ​× 10000, затем целую часть произведения Py
# Py​ × 10000 для файла А, во второй строке – аналогичные данные для файла B.




