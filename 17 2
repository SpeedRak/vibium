spisok1 = open('17 2 file.txt')
spisok2 = open('17 2 file.txt')
min37 = []
max73 = []
for i in spisok1:
    if int(i) % 37 == 0:
        min37 += [int(i)]
    if int(i) % 73 == 0:
        max73 += [int(i)]
x0 = int(spisok2.readline())
summa = []
k = 0
for x in spisok2:
    if (max(max73) < int(x0) < min(min37) and not(max(max73) < int(x) < min(min37))) or (max(max73) < int(x) < min(min37) and not(max(max73) < int(x0) < min(min37))):
        #print(min(min37), max(max73))
        #print(x0, x)
        k += 1
        summa += [int(x0) + int(x)]
    x0 = int(x)
print(k, min(summa))

#В файле содержится последовательность целых чисел, не превышающих 1000. Определите количество пар последовательности, в которых только одно число находится между значениями минимального кратного 37 и максимального кратного 73. Гарантируется, что такая пара в последовательности есть.

#В ответе запишите количество найденных пар и минимальную сумму элементов среди таких пар. В данной задаче под парой подразумевается два идущих подряд элемента последовательности.