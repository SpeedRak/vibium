f = open('24 1 file.txt').readline()
f = f.replace('BA', '*')
f = f.replace('DA', '*')
res = 0
slovo = ''
for i in range(len(f)):
    slovo += f[i]
    if slovo[-1] == '*':
        res = max(res, len(slovo))
    else:
        slovo = ''
print(res)

# Текстовый файл состоит из символов A, B, и D.
# Определите максимальное количество идущих подряд
# пар символов BA или DA в прилагаемом файле.
# Искомая подпоследовательность должна состоять 
# только из пар BA, или только из пар DA, 
# или только из пар BA и DA.
