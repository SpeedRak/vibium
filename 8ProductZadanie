from itertools import product
s = 'ЩРЮИ'
s = sorted(s)
b = set()
i = 0
for x in product(s, repeat=5): #4096 всего
    a = ''.join(x)
    if a[0] == 'Щ' and a[-1] == 'И':
        i += 1
        print(i, a)
    else:
        i += 1
#Все 5-буквенные слова, составленные из букв Щ, Р, Ю, И, записаны
#в алфавитном порядке и пронумерованы.
#Вот начало списка:

#1. ИИИИИ
#2. ИИИИР
#3. ИИИИЩ
#4. ИИИИЮ
#5. ИИИРИ
#6. ИИИРР

#Под каким номером идёт последнее слово, которое начинается
#на букву Щ и заканчивается на букву И?
    