def f(n):
    n1 = n
    r = g(n, 3)
    if n1 % 2 == 0:
        r = '2' + r + g((int(r[-1], 10) * 2), 3)
    else:
        r = g((int(r[0])*2), 3) + r + '2'
    return int(r, 3)

def g(n, k):
    r = ''
    while n > 0:
        r = str(n%k) + r
        n //= k
    return r

for x in range(1, 1000):
    if f(x) > 100:
        print(f(x))
        break
#Автомат принимает на вход число N и преобразует его по следующему алгоритму.
#1. Число N переводится в троичную систему счисления,
#2. Полученная запись преобразуется по следующему алгоритму:
#a. Если число N четное, то слева дописывается 2, справа значение младшего разряда,
#увеличенного в 2 раза (в троичной системе счисления),
#b. Если число N нечетное, то справа дописывается 2, 
#слева удвоенное значение старшего разряда (в троичной системе счисления),
#3. Полученная троичная запись переводится в десятичную систему счисления и выводится на экран.
#Укажите минимальное значение большее 100, 
#которое может являться результатом работы автомата.