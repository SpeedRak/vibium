from itertools import permutations, product  #permutation(столько раз, сколько заявили); product декартова произведение
s = 'IGROK'
res = set()
for x in permutations(s):
    a = ''.join(x) 
    if a[0] != 'K' and 'ROK' not in a:
        res.add(a)
        print(a)
print(len(res))
#Вася составляет 5-буквенные коды из букв И, Г, Р, О, К.
#Каждую букву нужно использовать ровно раз, при этом код не может начинаться с
#буквы К и не может содержать сочетания рок.
#сколько различных кодов может составить Вася?

